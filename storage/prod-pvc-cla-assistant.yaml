---

# Storage account for CLA assistant Mongo DB
apiVersion: v1

kind: PersistentVolumeClaim
metadata:
  name: cla-assistant
  namespace: cla
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: managed-premium-custom
  resources:
    requests:
      storage: 50Gi
